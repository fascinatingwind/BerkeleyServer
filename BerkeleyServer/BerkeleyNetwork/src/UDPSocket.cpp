#include "UDPSocket.h"

#include <sys/socket.h>
#include <netinet/in.h>
#include <unistd.h>
#include <arpa/inet.h>

namespace Network {
    UDPSocket::UDPSocket(size_t port, const std::string &address) {

    }
}